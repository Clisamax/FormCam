import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
	container: {
		padding: 16,
		width: '100%',
		alignContent: 'center',
		justifyContent: 'center',
		borderRadius: 35,
	},
	content: {
		flexDirection: 'row',
		alignItems: 'center',
		justifyContent: 'center',
	},
});